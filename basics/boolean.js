// === - Equality operator
// !== - No equal operator
// < - less than operator
// > - Greater than operator
// <= less than or equal to operator
// >= Greater than or equal to operator

let temp = 180

if (temp <= 32) {
    console.log('It is freezing outside!')
}

if (temp >= 110) {
    console.log('It is way too hot outside!')
}

// Challenge area

//Create an age set to your age
let age = 6

//If 7 or under print message about child pricing
if (age <= 7) {
    console.log('Welcome, kids get in free!')
}

//If 65 or older print message about seinor discount
if (age >= 65) {
    console.log('Welcome, you get a senior discount!')
}
